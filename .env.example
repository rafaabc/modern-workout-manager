PORT=
JWT_SECRET=
MONGODB_URI=
NODE_ENV=
# Frontend-only (Vite). Leave empty when backend serves frontend (monolith).
# Set to backend URL when deploying frontend separately (e.g. https://api.example.com).
VITE_API_BASE_URL=
